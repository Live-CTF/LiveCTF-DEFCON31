{
    "log-driver": "syslog",
    "log-opts": {
        "syslog-facility": "daemon",
        "tag": "docker/{{ '{{.Name}}' }}"
    },
    "insecure-registries": [
        "{{ registry_host1 }}",
        "{{ registry_host2 }}"
    ]
}
